{
  "name": "@grin/app-grinlife",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "license": "MIT",
  "description": "GrinLife — the portfolio and roadmap site for Grin Legacy, GrinSocial and Serendipity.",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build && esbuild server/index.ts --platform=node --packages=external --bundle --format=esm --outdir=dist",
    "start": "NODE_ENV=production node dist/index.js",
    "preview": "vite preview --host 0.0.0.0"
  },
  "dependencies": {
    "@grin/content": "*",
    "@grin/ui": "*",
    "express": "^4.21.2",
    "react": "^19.2.1",
    "react-dom": "^19.2.1",
    "wouter": "^3.7.1"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.1.14",
    "@types/express": "^4.17.21",
    "esbuild": "^0.25.0",
    "tailwindcss": "^4.1.14"
  }
}
